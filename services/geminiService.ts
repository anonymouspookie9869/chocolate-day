
// AI Service removed to prevent build errors and remove API key dependency
export const generateLoveNote = async (context?: string) => {
  return "Your love is sweeter than any words I could ever generate.";
};
